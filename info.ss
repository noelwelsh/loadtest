#lang setup/infotab

(define name "Load Test")

(define blurb '((p "A simple load tester")))

(define repositories '("4.x"))
(define required-core-version "4.2.0.0")
(define categories '(devtools))
(define can-be-loaded-with 'all)

(define homepage "http://untyped.com/")
(define url "http://svn.untyped.com/")

(define primary-file "main.ss")
;;(define scribblings '(("scribblings/schemeunit.scrbl" (multi-page))))

(define release-notes '((p "Initial relase")))

